# 01_preprocess_alignment_tree.R
#
# Future role:
# Read configured whole-genome alignment and MCC tree inputs, match labels,
# prune/reorder data, encode the alignment, derive coordinate maps, and save
# reusable preprocessing objects for downstream scoring.

