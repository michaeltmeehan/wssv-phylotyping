# Raw Tree

Place the MCC tree file here.

Expected placeholder path from configuration:

`data/raw/tree/41SEQSUPDATEDEDITEDMCC`

The current pipeline expects a BEAST-style MCC tree whose internal nodes carry posterior support annotations such as `posterior=0.9159` in Nexus/BEAST comment blocks.
